<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>Edit Message</h1>

        <form method="POST" action="/edit/<%= message.id %>" class="message-form">
            <label for="messageUser">Name: </label>
            <input type="text" id="messageUser" name="messageUser" value="<%= message.user %>" required>

            <label for="messageText">Message: </label>
            <input type="text" id="messageText" name="messageText" value="<%= message.text %>" required>

            <button type="submit" class="submit-btn">Save</button>
        </form>
    </div>
</body>
</html>